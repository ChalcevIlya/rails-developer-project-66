# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  flash:
    log_in_req: You must log in
  sessions:
    create:
      logged_in: "Logged in as %{nickname}"
    destroy:
      logged_out: Logged out
  repositories:
    create:
      success: Repository was successfully added
      repo_not_found: Repository not found on GitHub
    index:
      title: Repositories List
      add_repository: Add repository
      name: Name
      language: Language
      clone_url: Clone url
      ssh_url: Ssh url
      last_check_status: Last check status
      no_repositories: No repositories added yet
    new:
      title: Add Repository
      repository: Repository
      select_repository: Select a repository...
      submit: Add
      back: Back to list
    show:
      run_check: Run check
      checks: Checks
      status: Status
      created_at: Created at
      more: More
      no_checks: No checks yet
    checks:
      create:
        check_started: Check started
      show:
        check: Check
        repository: Repository
        commit: Commit
        status: Status
        result: Result
        no_result: No result yet
        back: Back to repository
